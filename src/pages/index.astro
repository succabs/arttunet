---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
	<main>
		<p><marquee behavior="alternate" scrollamount="3">🛸 UNDER CONSTRUCTION 🛸</marquee></p>
	  
		<img class="portrait" src="/me.jpg" alt="Portrait of Arttu Nikkilä" />

			<h1>💾 Welcome to My Personal Homepage!</h1>
	  
			<p>Hi, my name is Arttu Nikkilä.</p>
	  
			<p>I create beautiful websites and ensure accurate analytics data.</p>
	  
			<p>
			  In my current role as a developer I adapt to a wide range of situations deeply in the <em>client interface</em>,
			  balancing technical expertise with customer-facing responsibilities in the field of eCommerce.
			</p>
	  
			<p>
			  From this site you can find answers to your deepest questions, included but not limited to the following:
			</p>
			<ul>
			  <li>💻 What can Arttu do work-wise?</li>
			  <li>🧲 What the heck is magnet fishing?</li>
			  <li>🎧 Is Arttu really a DJ?</li>
			  <li>📚 What are Arttu's favorite sci-fi books?</li>
			</ul>
	  
			<p>
			  If any of that sounds interesting — awesome! Take a look around and enjoy your stay :)
			</p>
	  
			<p>-Arttu</p>
	  </main>
	  
    <Footer />
	<script>
		const pages = ['home', 'experience', 'projects', 'skills', 'about', 'blog'];
		const visitedPages = JSON.parse(localStorage.getItem('visitedPages') || '{}');
	  
		// Mark current page as visited based on the referrer
		const currentPath = window.location.pathname.replace('/', '') || 'home';
		if (pages.includes(currentPath)) {
		  visitedPages[currentPath] = true;
		  localStorage.setItem('visitedPages', JSON.stringify(visitedPages));
		}
	  
		// Mark visited pages in the list
		const listItems = document.querySelectorAll('#quest-list li');
		listItems.forEach((item) => {
	  const el = item as HTMLElement;
	  const page = el.dataset.page;
	  if (page && visitedPages[page]) {
		el.style.textDecoration = 'line-through';
	  }
	});
	
	  
		// Show reward if all pages are visited
		const allVisited = pages.every((p) => visitedPages[p]);
		if (allVisited) {
			const rewardEl = document.getElementById('reward');
	if (allVisited && rewardEl) {
	  rewardEl.style.display = 'block';
	}
		}
	  </script>
  </body>
</html>
