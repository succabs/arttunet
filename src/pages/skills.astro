---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title="Skills | Arttu Nikkilä" description="Overview of Arttu Nikkilä's technical and professional skills" />
  </head>
  <body>
    <Header />
    <main>
      <h1>🛠️ Skills</h1>

      <section>
        <h2>Languages</h2>
        <ul>
          <li>HTML5</li>
          <li>CSS3</li>
          <li>JavaScript</li>
          <li>TypeScript</li>
          <li>Liquid</li>
          <li>Python</li>
          <li>Java</li>
        </ul>
      </section>

      <hr />

      <section>
        <h2>Frameworks & Libraries</h2>
        <ul>
          <li>React</li>
          <li>Astro</li>
          <li>jQuery</li>
          <li>TailwindCSS</li>
          <li>Bootstrap</li>
          <li>Flask</li>
        </ul>
      </section>

      <hr />

      <section>
        <h2>Tools & Other Stuff</h2>
        <ul>
          <li>Google Analytics</li>
          <li>Git</li>
          <li>Docker</li>
          <li>VSCode</li>
          <li>PostgreSQL</li>
          <li>Node.js</li>
          <li>Nginx</li>
          <li>Vite</li>
          <li>Figma</li>
          <li>Jira</li>
          <li>Teamwork</li>
          <li>Microsoft Teams</li>
          <li>Linux</li>
        </ul>
      </section>
    </main>
    <Footer />
    <script>
      const pages = ['home', 'experience', 'projects', 'skills', 'about', 'blog'];
      const visitedPages = JSON.parse(localStorage.getItem('visitedPages') || '{}');
    
      // Mark current page as visited based on the referrer
      const currentPath = window.location.pathname.replace('/', '') || 'home';
      if (pages.includes(currentPath)) {
        visitedPages[currentPath] = true;
        localStorage.setItem('visitedPages', JSON.stringify(visitedPages));
      }
    
      // Mark visited pages in the list
      const listItems = document.querySelectorAll('#quest-list li');
      listItems.forEach((item) => {
    const el = item as HTMLElement;
    const page = el.dataset.page;
    if (page && visitedPages[page]) {
      el.style.textDecoration = 'line-through';
    }
  });
  
    
      // Show reward if all pages are visited
      const allVisited = pages.every((p) => visitedPages[p]);
      if (allVisited) {
          const rewardEl = document.getElementById('reward');
  if (allVisited && rewardEl) {
    rewardEl.style.display = 'block';
  }
      }
    </script>
  </body>
</html>
